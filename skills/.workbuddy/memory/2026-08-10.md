# 2026-08-10 工作日志

## 跑通 7-skill 集合 (interest-island-invoice-automation v8.0.0)

### 测试范围
按 MANIFEST 链路全链路深测（用户确认）：mail-monitor → request-parse → pipeline → wecom-query → island-checker → invoice-create → (wecom-import 跳过)。

### 配置变更
- `invoice-mail-monitor/skill/config.yaml`：
  - email.account = `jiachun.guo@qlchat.com`
  - email.password = `hrFp17uONpfrM4jM`（阿里云授权码）
  - handoff.dir = `E:/File/.../skills/invoice_handoff`
- `invoice-request-parse/skill/config.yaml`：handoff.dir / output.dir 同上路径

### 通过状态
- mail-monitor：用户发测试邮件 → IMAP 抓取 → 5713B xlsx 提取 → 侧车 20260810_102959_27.json ✓
- request-parse：解析 1 笔（订单 9000000782190489 / 3904元 / 广州云课教育 / 91440101MA5A1B2）✓
- wecom-query：doc_url 用户提供的 e3_AWwAGAaLAFMCN4Gx1f3rFSpm5afm2；引擎 94ms；扫描 6033 行 found=false ✓
- island-checker：用户手动短信验证码登录；session 有效；详情已支付未开票 ✓
- invoice-create：构造 PDF base64 输入；confirm=false 安全门；10 字段填全未提交 ✓
- wecom-import：用户选择跳过（保护生产企微表）

### 发现 bug
1. **P0 can_invoice 语义不一致**：island-checker 脚本 `can_invoice = hasInvoiceSection`（= 已开票），但 pipeline.yaml 注释 `can_invoice: can_proceed # true→继续开票`（语义相反）。未开票订单 can_invoice=false 会被 pipeline 误跳过。
2. **P1 invoice-create PDF 真上传**：confirm=false 时 PDF 仍上传到 `media.qianliaowang.com/xqd/admin/ql-upload/`，会留下孤立文件。
3. **P2 wecom 两脚本 DEFAULT_DOC_URL=""**：必须通过输入 json doc_url 传入。

### 凭证使用
阿里云邮箱 + 授权码已用于本测试。是否轮换建议待用户决定。
