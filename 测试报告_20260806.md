# 兴趣岛开票自动化 Skill 集 — 测试报告

> 测试日期：2026-08-06 ｜ 测试角色：非技术人员视角 ｜ 测试范围：全部 7 个 skill（8 步流程）
> **更新（同日 15:15）**：P0 Bug 与 P1 卡点已全部修复并同步到全局部署，回归验证通过。真实环境实测完成：邮箱连接 ✅、企微查重双分支 ✅、订单核验 ✅。见文末「六、修改记录」与「七、真实环境实测记录」。

---

## 一、结论总览

| 维度 | 结果 |
|------|------|
| 部署 | ✅ 7 个 skill 全部全局部署到 `~/.workbuddy/skills/`，Python 运行环境（venv+依赖）已就绪 |
| 自动化测试 | ✅ 46 项检查通过（18 单测 + 14 单测 + 分类 4 项 + 模拟端到端 + 构建/语法/环境检查等） |
| 核心功能 | ✅ 修复后回归通过：抬头解析 Bug 已修，首次使用卡点文档已补齐 |
| 真实环境 | ✅ 邮箱真实连接成功；企微查重（只读）双分支实测通过；兴趣岛订单核验（只读）实测通过 |
| 待验收 | ⏳ 步骤 7（发票新建，需真实 PDF+开票场景）、步骤 8（企微录入，写入类，等用户确认） |

---

## 二、逐 Skill 测试明细

### 步骤 1-2：invoice-mail-monitor（邮件监控）
| 检查项 | 结果 |
|--------|------|
| 配置加载（config.yaml） | ✅ 正常，handoff 已指向项目内目录 |
| 密码脱敏（日志不泄露凭证） | ✅ 显示为 `******` |
| 邮件三分类（invoice/other/uncertain） | ✅ 4/4 用例通过 |
| 模拟端到端：开票邮件→取附件→写侧车→标已读 | ✅ PASS |
| 连接失败安全退出（退出码 0，不误标已读） | ✅ PASS（无凭证时实测） |
| **真实拉取阿里云邮箱** | ⏳ 等凭证（配置当前为占位符） |

### 步骤 3：invoice-request-parse（请求解析）
| 检查项 | 结果 |
|--------|------|
| 自带 pytest 单测 | ✅ 17/17 通过 |
| 端到端：读侧车→解析 xlsx→校验→去重→双报告→归档 | ✅ PASS |
| 报告结构（json 顶层字段供下游消费） | ✅ 与 invoice-pipeline 预期一致 |
| 抬头/税号提取 | ❌ **发现 Bug（见问题 1）** |

### 步骤 4：wecom-invoice-query（企微查重，只读）
| 检查项 | 结果 |
|--------|------|
| setup.py 环境检查 | ✅ dev-browser / Python 就绪 |
| JS 语法（原始 + 合并产物） | ✅ PASS |
| 公共库自检 verify_lib | ✅ 7/7 通过 |
| 真实查询企微文档 | ⏳ 需扫码登录后验收（只读操作） |

### 步骤 5：order-invoice-checker（订单核验，只读）
| 检查项 | 结果 |
|--------|------|
| JS 语法 + config JSON 合法性 | ✅ PASS |
| 真实核验兴趣岛订单 | ⏳ 需扫码登录后验收（只读操作） |

### invoice-pipeline（主编排，纯文档）
| 检查项 | 结果 |
|--------|------|
| 与上游 .json 报告的字段/结构对接 | ✅ 一致 |
| 6 阶段流程 + 人工断点 + 安全门 | ✅ 设计自洽 |
| 文档一致性问题 | ⚠️ 若干小问题（见问题 8/12） |

### 步骤 7：invoice-create（发票新建，安全门默认不提交）
| 检查项 | 结果 |
|--------|------|
| JS 语法 + config JSON 合法性 | ✅ PASS |
| 安全门（confirm 默认 false） | ✅ 配置确认 |
| 真实填写弹窗 | ⏳ 需扫码登录后验收（本次不提交任何数据） |

### 步骤 8：wecom-invoice-import（企微录入）
| 检查项 | 结果 |
|--------|------|
| setup.py 环境检查 | ✅ openpyxl / dev-browser 就绪 |
| 自带 pytest 单测 | ✅ 14/14 通过 |
| Excel→TSV 实测（日期格式/金额/列对齐/订单号） | ✅ 8 列输出正确，JS 端补 2 列凑 10 列 |
| 真实录入企微表格 | ⏳ 需扫码登录后验收（写入操作，未测） |

---

## 三、发现的问题（按严重程度排序）

> 状态说明：🔴 P0 与 🟠 P1 已于当日修复并同步部署，回归验证通过；🟡 P2 为文档小瑕疵，其中影响使用的（问题 7/8/9/10/11/12）已一并顺手修复，剩余 1 项为设计边界说明。

### 🔴 P0 — 会影响开票正确性 ✅ 已修复

**问题 1：发票抬头解析错列（解析成了"商品名称"）**

- **现象**：当表格表头同时存在「商品名称」和「公司名称/姓名」两列时，发票抬头被解析成商品名称（如"课程A"），而不是公司名称。
- **对用户的影响**：下游开票时抬头会填错，发票抬头打错公司——这是开票流程里最不能错的信息。
- **为什么没被发现**：自带的回归测试用的表头是「购买方名称」（别名更长，能正确匹配），没覆盖真实表格的「公司名称/姓名」表头（别名只剩"名称"两个字，与「商品名称」冲突时选错了列）。
- **修复建议（按成本从低到高）**：
  1. 在 `config.yaml` 的 `title` 别名中**删除"名称"**，增加 `"公司名称"`（最小改动，避免"名称"误伤「商品名称」）；
  2. 同时在 `tests/` 中补一个「公司名称/姓名」表头的回归测试用例，防止再犯。

### 🟠 P1 — 非技术用户照着文档操作会被卡住 ✅ 已修复

**问题 2：安装指引漏了 PyYAML 依赖**
- README/两个 Python skill 的 SKILL.md 只写了 `pip install openpyxl pytest`，但代码实际还需要 `PyYAML`。用户照文档装完直接跑会报 `ModuleNotFoundError: No module named 'yaml'`。
- 建议：SKILL.md 安装命令改为 `pip install -r requirements.txt`（根目录已有完整清单），或补上 PyYAML。

**问题 3：文档没写"怎么创建 venv"**
- SKILL.md 直接写 `venv/Scripts/python.exe -m skill.src.monitor`，但没告诉用户 venv 怎么建、依赖怎么装。第一次接触的用户会卡在这一步。
- 建议：在每个 Python skill 的 SKILL.md 里加一段"首次使用 3 步"（建 venv → 装依赖 → 填配置）。

**问题 4：邮箱凭证 / 交接目录是占位符，且没有醒目的"首次使用清单"**
- `config.yaml` 里账号密码、交接目录都是 `${...}` 占位符。用户不填就直接跑，结果是"静默失败"（连接失败安全退出，看起来像没反应）。
- 建议：SKILL.md 顶部加一个醒目的"⚠️ 首次使用必做"清单：① 填邮箱账号密码 ② 填交接目录 ③ 先跑环境检查。

**问题 5：SKILL.md 名称与目录名不一致**
- `order-invoice-checker` 的 SKILL.md 里 `name` 写的是 `interest_island_invoice_checker`；`invoice-create` 写的是 `interest_island_invoice_create`。
- 影响：如果按 name 检索/调用 skill，会找不到或对不上目录名。建议统一。

**问题 6：浏览器类 skill 的"构建"步骤在部署后失效**
- SKILL.md 写"在仓库根目录执行 `python tools/build_all.py`"，但全局部署后 skill 目录里没有 `tools/`。
- 本次部署已把构建产物（`build/*.merged.js`）放到每个 skill 目录里，可直接运行；建议 SKILL.md 补一句说明"部署版自带构建产物，可直接 `dev-browser run build/xxx.merged.js`"。

### 🟡 P2 — 文档小瑕疵/边界场景（问题 7-12 已顺手修复，13 为设计边界说明）

- **问题 7** ✅：`order-invoice-checker/SKILL.md` 的"文件结构"写的是 `interest_island_invoice_checker/skills/order-invoice-checker/`，与仓库实际路径不符。
- **问题 8** ✅：`invoice-pipeline` 阶段 0.5 写的报告路径是 `<日期>_发票邮件报告.md`，实际上游生成的是 `{YYYY.M.D}_{HH-mm}_发票邮件报告.md`（带时间），按前者找文件会找不到。
- **问题 9** ✅：`wecom-invoice-query/SKILL.md` 的"批量查询"示例代码用了 `const` 和箭头函数，与文档自己声明的 QuickJS 约束（用 `var` / `function`）矛盾，AI 直接照抄可能在沙箱里报错。
- **问题 10** ✅：`wecom-invoice-import/SKILL.md` 说"脚本会自动在每行前补 2 个空 tab"，实际是 JS 脚本补的（`read_excel_to_tsv.py` 输出 8 列）；列号建议注明是"引擎 0-based"以免误会。
- **问题 11** ✅：README 写"含 5 个 pytest 测试用例"，实际是 5 个测试文件共 **17** 个用例。
- **问题 12** ✅：`invoice-mail-monitor/SKILL.md` 的侧车字段列表漏了 `body_text`（代码实际会写入）。
- **问题 13（边界，未改）**：`invoice-pipeline` 判断"个人/企业"靠抬头里有没有"个人"字样——如果抬头是"张三"（不带"个人"后缀），会被当成企业并要求税号，可能卡住。建议把规则写清楚或加一步确认。此项为编排层设计行为，阶段 3 出卡片前已有确认机制兜底，暂不改动。

---

## 六、修改记录（2026-08-06）

| # | 类型 | 修改内容 | 涉及文件 |
|---|------|---------|---------|
| 1 | 🔴 P0 | title 别名删"名称"、增"公司名称"，修复「商品名称」与「公司名称/姓名」列冲突导致抬头解析错列 | `invoice-request-parse/skill/config.yaml`、`skill/src/table_parser.py` |
| 2 | 🔴 P0 | 新增回归测试 `test_parse_xlsx_real_header_company_name`（真实表头变体） | `invoice-request-parse/tests/conftest.py`、`tests/test_table_parser.py` |
| 3 | 🟠 P1 | SKILL.md 补「首次使用必做」清单：venv 创建、`requirements.txt` 依赖（含 PyYAML）、凭证/交接目录填写、验证步骤 | `invoice-mail-monitor/SKILL.md`、`invoice-request-parse/SKILL.md` |
| 4 | 🟠 P1 | SKILL.md `name` 与目录名统一（`interest_island_invoice_checker`→`order-invoice-checker`、`interest_island_invoice_create`→`invoice-create`） | `order-invoice-checker/SKILL.md`、`invoice-create/SKILL.md` |
| 5 | 🟠 P1 | 4 个浏览器 skill 的 SKILL.md 补「部署版自带构建产物，无需再执行 build_all.py」说明 | `wecom-invoice-query`/`order-invoice-checker`/`invoice-create`/`wecom-invoice-import` 的 SKILL.md |
| 6 | 🟡 P2 | 文件结构路径修正、报告文件名统一、批量查询示例改 QuickJS 兼容写法、补 tab 说明与列号标注、README 依赖/用例数修正 | `README.md` 及各 SKILL.md |

**回归验证结果**：request-parse 单测 18/18 ✅（含新回归用例）、wecom-invoice-import 14/14 ✅、真实表头端到端解析 PASS（抬头正确）、mail-monitor 分类 PASS、构建产物重新生成 ✅、7 个 skill 名称一致性 ✅。

---

## 七、真实环境实测记录（2026-08-06 15:15）

> 使用真实凭证与真实系统（用户授权，只读操作；写入类步骤 7/8 未执行）。

### 1. 阿里云邮箱真实连接（invoice-mail-monitor）
- 连接 `imap.qiye.aliyun.com:993` ✅ 登录成功（账号脱敏 `ji***@qlchat.com`）
- 拉取结果：收件箱 **0 封未读**（邮箱真实状态，无侧车产出）
- 结论：凭证有效、IMAP 链路全通；「标已读」分支待邮箱有未读开票邮件时自然验证

### 2. 企微查重（wecom-invoice-query，只读）✅ 双分支
- 打开表格「副本-兴趣岛开票登记(1)」，引擎就绪 50ms / Sheet 就绪 62ms（轮询生效）
- **未开票分支**：查 `9999000000000009` → 遍历 **6033 行** → `found: false`（未开票，可继续开票）✅
- **已开票分支**：查真实订单 `9000000667403148` → 命中第 3 行，返回完整记录：日期 `2026/3/12`、发票号 `26442000002670431086`、抬头 `黄雪梅（个人）`、金额 `1904` ✅

### 3. 兴趣岛订单核验（order-invoice-checker，只读）✅ 双分支
- 登录态有效（`/order/financial`），Vue 组件直驱查询成功
- 查 `9999000000000004`（SKILL.md 测试订单）→ 系统返回"暂无数据" → 正确输出 `query_status: success / result_status: manual_review`
- 查真实已开票订单 `9000000667403148` → 命中详情 → 滚动 `el-drawer__body` 检测发票 → **`result_status: invoiced` / "已开票：电子普通发票 - 黄雪梅"** ✅（滚动修复逻辑生效，`hasInvoice: true`）
- 结论：脚本全流程（登录检测→Vue 直驱→清空筛选→详情滚动→判定）跑通，`manual_review` 与 `invoiced` 两分支实测通过；`not_invoiced` 分支待真实未开票订单验证
- SKILL.md 已加注「开发期测试订单号真实系统可能不存在」

### 3.5 邮件全链路实测（步骤 1→2→3→4→5，真实邮件）
- 用户发测试邮件（主题「发票申请-徐素.xlsx」）→ monitor 拉到 UID 25，提取附件 18789 字节并标已读 ✅（**顺带验证 readonly 模式下 STORE 标已读可用**）
- request-parse 解析出：订单 `9001000782190489`、金额 18880 元、抬头 **徐素**（个人）、备注「【全面升级】养元活血太极营」→ 报告 `.md/.json` 生成、侧车归档 processed/ ✅
- **P0 修复在真实数据上验证**：抬头正确解析为"徐素"（非商品名称）✅
- 企微查重 `9001000782190489` → `found: false`（未开票，可继续）✅
- 兴趣岛核验 `9001000782190489` → **查无此订单**（`manual_review`）→ 用户提供真实订单号替代

### 3.6 真实订单全链路（用户提供 `9000000788825207`）
- 企微查重 → `found: false`（未开票）✅
- 兴趣岛核验 → **`not_invoiced`**："未找到发票信息，可以继续进入税务局开票流程" ✅（**三判定分支全部实测完成**：manual_review / invoiced / not_invoiced）
- 阶段 3：开票信息卡已渲染（沿用邮件解析字段：抬头徐素/个人、金额 18880 元、项目【全面升级】养元活血太极营、订单号 9000000788825207）→ **人工断点**，等用户税务局开票后返回 PDF 进入阶段 4/5

### 4. 待验收项（写入类，未执行）
- **步骤 7 invoice-create**：需真实开票场景 + 发票 PDF（默认 `confirm=false` 安全模式，只填不提交）
- **步骤 8 wecom-invoice-import**：会真实写入企微表格，等用户明确确认后再做

### 4.5 步骤 7/8 安全模式实测（用户无 PDF，用占位 PDF + 重复数据验证）
- **invoice-create（步骤 7，安全模式 confirm=false）** ✅
  - 用占位 PDF（最小 PDF 字节 base64）填写真实订单 `9000000788825207` 的开票弹窗
  - 全字段自动填充：所属品类「懒人吃瘦」、商品名称「35天断食精控塑形营」、用户ID「10444712(Jessica未之)」（**真实 API 数据**）
  - 金额 18880.00、发票类型「电子普通发票」、抬头类型「个人/非企业」、抬头「徐素」全部正确填入
  - PDF 上传通路走通（base64→atob→File→DataTransfer→handleChange → fileListLength=1）
  - 截图 `dialog_filled_9000000788825207_*.png` 证据留存
  - **安全门生效**：`SAFETY: confirm=false, NOT clicking 确定键` → `result_status: filled_not_submitted / confirm_executed=false`
- **wecom-invoice-import（步骤 8，dedup_blocked 分支）** ✅
  - 构造「信息汇总表」Excel，发票号用企微表已存在的 `26442000002670431086`（来自 row3）
  - `read_excel_to_tsv.py` 生成 8 列 TSV 正确（日期 `2026/3/12`、发票号、票种、名称、税号、金额、订单号）
  - 8 步流程跑到第 3 步（引擎就绪+全表查重）→ 现有 60 条记录，TSV 1 条，**重复 1 条，新增 0 条**
  - 输出 `status: "dedup_blocked" / duplicates: ["26442000002670431086"]`，**零写入**

### 4.6 真实发票 PDF 实测（步骤 7，用户找到真实数电发票）
- 真实 PDF：`dzfp_26442000008713874926_世碧德环境科技（上海）有限公司_20260730100121.pdf`（104960 字节，数电普通发票）
- PDF 提取（pypdf）：发票号 `26442000008713874926`、抬头「世碧德环境科技（上海）有限公司」、税号 `91310000322172892J`、价税合计 `5580.00`、备注订单号 `9000000766731251`
- invoice-create 安全模式（confirm=false）全流程 ✅：
  - 订单自动填充（**真实 API**）：品类「瑜伽」、商品「45天身心深度调养计划」、用户「52382100883(may 雪梅)」
  - 金额 5580.00 / 电子普通发票 / 企业 / 抬头 / 税号 全部正确填入
  - **真实 PDF base64 上传通路验证**：104960 字节 → base64(139948) → atob → File → DataTransfer → handleChange → `fileListLength=1`（证明 SKILL.md 声称的「readFile utf8 截断二进制」问题在 base64 通路下 binary-safe 解决）
  - 截图 `dialog_filled_9000000766731251_1786004225837.png`；`confirm_executed=false`，未点确定键
- 途中登录态过期：脚本正确降级 `login_required`（未乱填/未提交），重新扫码后正常 ✅

### 4.7 新功能开发：新建发票备注栏自动填订单号（invoice-create 增强）
- **需求**：每次新建订单，弹窗「备注」栏自动输入 `订单号：{真实订单号}`；调用方传 `remarks` 时追加其后
- **实现**（`automation/interest_island_invoice_create.js`）：
  - 新增 `SET_REMARKS` 步骤（企业税号后、PDF 上传前）：备注 = `订单号：{order_id}`（+ `；{remarks}` 若有）
  - 输入校验：`remarks` ≤ 400 字符
  - 输出 `filled_fields.remarks` 记录实际填入值
- **过程中发现并修复 1 个 bug**：备注栏是 `<textarea>`，原 `setInputValueInDialog` 用 `HTMLInputElement.prototype` 的 value setter 会抛 `TypeError: Illegal invocation`；修复为按元素类型（textarea/input）选择正确 prototype setter
- **实测**（真实订单 9000000766731251，confirm=false）：`SET_REMARKS ok, value="订单号：9000000766731251"` → 截图 `dialog_filled_9000000766731251_1786005226073.png` → `filled_not_submitted / confirm_executed=false` ✅
- 文档同步：SKILL.md 输入表/工作原理/已知限制（删除"备注未处理"）、selectors.json 备注定义、构建产物、部署目录全部更新

### 5. 全链路总结
✅ **8 步全部真实环境实测**：
- 1-2 邮件监控（真实邮箱 + 真实邮件 + 标已读）
- 3 解析（真实表格 + 报告生成 + 侧车归档）
- 4 企微查重（双分支：未开票/已开票）
- 5 兴趣岛核验（三分支：manual_review/invoiced/not_invoiced）
- 6 开票信息卡渲染（人工断点）
- 7 发票新建（占位 PDF + **真实 PDF** 双验证，confirm=false 零风险）
- 8 企微录入（dedup_blocked 验证，零写入）

---

## 四、真实环境待办（需要你配合）

| 事项 | 说明 |
|------|------|
| 1. 提供阿里云邮箱账号+密码/授权码 | 用于实测步骤 1-2 真实拉邮件（本次已就绪，凭证一到即可跑） |
| 2. 企微文档扫码登录 | 步骤 4 查重 / 步骤 8 录入需要（只读验收可先做步骤 4） |
| 3. 兴趣岛系统扫码登录 | 步骤 5 核验 / 步骤 7 新建需要（步骤 7 默认不提交，可安全验收） |

---

## 五、部署说明（本次已完成）

- 位置：`C:\Users\EDY\.workbuddy\skills\{skill 名}`
- 两个 Python skill 已建好 venv 并安装 `PyYAML / openpyxl / pytest`
- 4 个浏览器 skill 已放入合并构建产物 `build/*.merged.js`（免构建直接运行）
- 交接目录已指向：`<项目>/handoff/`（pending / processed / reports / processed_emails.json）
- 邮箱凭证仍为占位符，等你提供后填入 `invoice-mail-monitor/skill/config.yaml`
